services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    environment:
      TURSO_DATABASE_URL: ${TURSO_DATABASE_URL}
      TURSO_AUTH_TOKEN: ${TURSO_AUTH_TOKEN}
    ports:
      - "3000:3000"

  web:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - api
